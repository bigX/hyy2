<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hiyaya"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 此权限用来显示反馈崩溃信息对话框 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 判断堆栈中是否有本应用的权限 -->
    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hi"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.hiyaya.activity.MainActivity"
            android:label="@string/app_name" >
        </activity>
      <!--   <activity
            android:name="com.hiyaya.activity.SplashActivity"
            android:theme="@style/NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->
        
         <activity
            android:name="com.hiyaya0.activity.SplashActivity"
            android:theme="@style/NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.hiyaya.activity.CardActivity"
            android:label="@string/app_name"
            android:theme="@style/NoTitleBar" >
        </activity>
        <activity
            android:name="com.hiyaya.activity.Card2Activity"
            android:label="@string/app_name"
            android:theme="@style/NoTitleBar" >
        </activity>
        <activity
            android:name="com.hiyaya.activity.MeActivity"
            android:label="@string/app_name"
            android:theme="@style/NoTitleBar" >
        </activity>
        <activity
            android:name="com.hiyaya.activity.IndexActivity"
            android:theme="@style/NoTitleBar" >
        </activity>
         <activity
            android:name="com.hiyaya0.activity.IndexActivity"
            android:theme="@style/NoTitleBar" >
        </activity>
         <activity
            android:name="com.hiyaya0.activity.RankActivity"
            android:theme="@style/NoTitleBar" >
        </activity>
        <activity
            android:name="com.hiyaya.activity.LoginActivity"
            android:theme="@style/NoTitleBar" >
        </activity>
    </application>

</manifest>